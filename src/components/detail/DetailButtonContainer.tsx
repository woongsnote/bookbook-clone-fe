const DetailButtonContainer = ({ children }: { children: React.ReactNode }) => {
  return <div className="flex items-center justify-center m-4 mt-10">{children}</div>;
};

export default DetailButtonContainer;
